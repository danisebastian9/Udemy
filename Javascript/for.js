for (let i = 0; i < 10; i++) {
    //console.log(i);
}


// Accediendo arrays. 
const numeros = [1, 2, 3, 4, 5]
console.log(numeros[0]);

for (let arrayi = 0; arrayi < numeros.length; arrayi++) {
    //console.log(numeros[arrayi]); // Prints the entire list values 1 to 5.
    console.log(arrayi);            // Prints the loop length 0 to 4. 
}

